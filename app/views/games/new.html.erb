<div class="min-h-screen bg-green-800 flex items-center justify-center">
  <div class="bg-white rounded-lg shadow-xl p-8 max-w-md w-full">
    <h1 class="text-3xl font-bold text-center mb-6 text-gray-800">Blackjack</h1>

    <% if flash[:alert] %>
      <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded mb-4">
        <%= flash[:alert] %>
      </div>
    <% end %>

    <div class="mb-6">
      <div class="text-center">
        <p class="text-gray-600 text-sm mb-2">Your Balance</p>
        <p class="text-4xl font-bold <%= @balance > 0 ? 'text-green-600' : 'text-red-600' %>">$<%= @balance %></p>
      </div>
    </div>

    <% if @balance > 0 %>
      <%= form_with url: games_path, method: :post, class: "space-y-4" do |f| %>
        <div>
          <label for="bet" class="block text-sm font-medium text-gray-700 mb-2">
            Place Your Bet
          </label>
          <input
            type="number"
            name="bet"
            id="bet"
            min="1"
            max="<%= @balance %>"
            value="<%= [ 100, @balance ].min %>"
            class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-green-500 focus:border-transparent"
            required
          />
          <p class="text-xs text-gray-500 mt-1">Minimum bet: $1, Maximum bet: $<%= @balance %></p>
        </div>

        <button
          type="submit"
          class="w-full bg-green-600 hover:bg-green-700 text-white font-bold py-3 px-4 rounded-lg transition duration-200"
        >
          Deal Cards
        </button>
      <% end %>
    <% else %>
      <div class="bg-red-50 border-2 border-red-200 rounded-lg p-6 mb-4">
        <h2 class="text-2xl font-bold text-red-600 text-center mb-2">Game Over!</h2>
        <p class="text-gray-700 text-center mb-4">You're out of money. Better luck next time!</p>
      </div>

      <%= link_to "Start New Game", new_game_path(reset: true),
          class: "block w-full bg-blue-600 hover:bg-blue-700 text-white font-bold py-3 px-4 rounded-lg transition duration-200 text-center" %>
    <% end %>

    <div class="mt-6 pt-6 border-t border-gray-200">
      <h2 class="text-sm font-semibold text-gray-700 mb-2">How to Play:</h2>
      <ul class="text-xs text-gray-600 space-y-1">
        <li>• Get as close to 21 as possible without going over</li>
        <li>• Face cards (J, Q, K) are worth 10</li>
        <li>• Aces are worth 1 or 11</li>
        <li>• Dealer must hit on 16 or less, stand on 17 or more</li>
        <li>• Blackjack (21 in 2 cards) pays 3:2</li>
      </ul>
      <p class="text-xs text-gray-500 mt-2">
        <%= link_to "Official Bicycle Cards Rules", "https://bicyclecards.com/how-to-play/blackjack/", target: "_blank", class: "text-blue-600 hover:text-blue-800 underline" %>
      </p>
    </div>
  </div>
</div>
